# 2026-05-03

## Removed
- 移除 `chat_reference_snapshots` 持久化链路及相关代码
  - 移除 `types/chat.d.ts` 中的 `ChatReferenceSnapshot` 类型定义
  - 移除 `sqlite.ts` 中的 snapshot 相关 SQL 语句和方法（`getReferenceSnapshots`、`getReferenceSnapshotByDocumentId`、`upsertReferenceSnapshots`）
  - 移除 `database/service.mts` 中的 `chat_reference_snapshots` 表和索引定义
  - 删除 `referenceSnapshot.ts` 文件（包含 `persistReferenceSnapshots` 函数）
  - 简化 `referenceResolver.ts`，移除对快照存储的依赖，仅保留编辑器上下文解析
  - 简化 `fileReferenceContext.ts`，移除快照参数，改为生成引用索引块
  - 更新 `useChatStream.ts`，移除快照加载逻辑
  - 更新 `BChatSidebar/index.vue`，移除快照相关调用
  - 更新相关测试文件

## Fixed
- 修复语音录音停止后系统仍显示录音中的问题
  - 在 `useVoiceSession.ts` 中添加转写失败的错误处理，防止 Promise 链中断
  - 在 `useVoiceRecorder.ts` 中添加音频段处理失败的错误处理
  - 在 `stop()` 方法中添加 5 秒超时保护机制，确保资源能够被释放
  - 问题原因：转写失败时 Promise reject 导致 `segmentDeliveryQueue` 永远等待，`cleanupResources()` 无法执行

## Changed
- 简化 file-ref token 格式，让大模型更容易理解
  - 新格式：`{{@fileName:startLine-endLine}}`，替代旧格式 `{{file-ref:documentId|fileName|startLine|endLine}}`
  - 更新 `useFileReference.ts` token 生成逻辑
  - 更新 `chipResolver.ts` token 解析逻辑（仅支持新格式）
  - 更新 `messageHelper.ts` 消息处理（仅支持新格式）
  - 更新 `fileReferenceContext.ts` 引用索引块格式为 `[documentId] path (lines x-y)`
  - 更新 `BubblePartText.vue` 渲染逻辑（仅支持新格式）
  - 移除旧格式兼容代码
  - 更新相关测试文件

- 调整语音输入相关测试用例，使其与当前 `useVoiceSession`、`VoiceInput` 和聊天侧边栏接线实现保持一致
