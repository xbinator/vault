# 2026-05-03

## Fixed
- 修复语音录音停止后系统仍显示录音中的问题
  - 在 `useVoiceSession.ts` 中添加转写失败的错误处理，防止 Promise 链中断
  - 在 `useVoiceRecorder.ts` 中添加音频段处理失败的错误处理
  - 在 `stop()` 方法中添加 5 秒超时保护机制，确保资源能够被释放
  - 问题原因：转写失败时 Promise reject 导致 `segmentDeliveryQueue` 永远等待，`cleanupResources()` 无法执行

## Changed
- 调整语音输入相关测试用例，使其与当前 `useVoiceSession`、`VoiceInput` 和聊天侧边栏接线实现保持一致
