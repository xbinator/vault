# 2026-04-24

## Added
- 新增文件引用聊天上下文设计文档，明确 `references`、`snapshot`、`summary` 三层数据模型与 `documentId` 主键方案。

## Changed
- 明确文件引用消息的固定三档上下文注入阈值，并约束 `excerpt` 仅用于 UI 预览、不参与模型上下文构建。
- 补充文件引用上下文设计文档中的快照复用粒度、孤儿数据策略、`line` 解析契约、token 草稿级还原范围与阶段拆分边界。
- 补充 `parseLineRange` 的 1-based 行号与解析失败降级策略，并恢复文档中的 fallback、实现阶段和测试建议章节。
