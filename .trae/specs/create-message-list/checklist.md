- [x] `types.ts` 中的 `Message` 接口已正确扩展（包含 id、images 等字段）。
- [x] `Messages.vue` 内部能根据 `role` 正确使用 `BBubbleText` 呈现左右气泡布局。
- [x] 单条消息支持展示纯文本及图片等多模态内容。
- [x] 鼠标悬浮消息时，正确出现“复制”、“编辑”、“删除”、“重新生成”等操作按钮（按角色区分显隐）。
- [x] `Messages.vue` 能接收并渲染消息列表数据，正确抛出交互事件。
- [x] `BChat/index.vue` 成功集成了消息列表，且滚动行为正常。
