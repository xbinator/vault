# 2026-04-29

## Changed
- 修正话题自动命名设计文档中的快照采集时序、`chatSidebarActiveSessionId` 空值类型，以及 `awaiting_user_input` 提前触发问题。
- 实现会话首轮自动命名能力，新增 `autoname` 服务配置、标题更新存储接口与侧边栏异步命名流程。
- 新增聊天侧边栏 `/` 命令设计文档，定义行首触发的统一命令面板及 `/model`、`/usage`、`/new`、`/clear` 第一版方案。
- 补充聊天侧边栏 `/` 命令设计细节，明确 `/usage` 持久化数据来源、多行删除边界、模型选择器可编程打开能力与关键回归测试点。
- 收敛聊天侧边栏 `/` 命令设计决策，明确 `ModelSelector.open()` 推荐方案、无命令配置时的编辑器回退行为，以及 `/usage` 面板的即时打开、加载态与内联错误态。
- 新增聊天侧边栏 slash 命令实现计划，拆分编辑器触发、模型选择器复用、usage 面板与回归测试任务。
- 调整会话视图“回到底部”悬浮按钮的主题样式，复用悬浮指示器变量并增强暗黑模式下的边界与阴影对比。
