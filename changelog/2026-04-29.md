# 2026-04-29

## Changed
- 修正话题自动命名设计文档中的快照采集时序、`chatSidebarActiveSessionId` 空值类型，以及 `awaiting_user_input` 提前触发问题。
- 实现会话首轮自动命名能力，新增 `autoname` 服务配置、标题更新存储接口与侧边栏异步命名流程。
- 新增聊天侧边栏 `/` 命令设计文档，定义行首触发的统一命令面板及 `/model`、`/usage`、`/new`、`/clear` 第一版方案。
- 补充聊天侧边栏 `/` 命令设计细节，明确 `/usage` 持久化数据来源、多行删除边界、模型选择器可编程打开能力与关键回归测试点。
- 收敛聊天侧边栏 `/` 命令设计决策，明确 `ModelSelector.open()` 推荐方案、无命令配置时的编辑器回退行为，以及 `/usage` 面板的即时打开、加载态与内联错误态。
- 新增聊天侧边栏 slash 命令实现计划，拆分编辑器触发、模型选择器复用、usage 面板与回归测试任务。
- 为 BPromptEditor 增加行首 slash 命令菜单、键盘导航、blur 收起与普通文本回退行为。
- 为聊天侧边栏接入 `/model`、`/usage`、`/new`、`/clear` 四条 slash 命令，并复用现有模型选择器打开路径。
- 新增会话 usage 面板以及持久化 usage 读取链路，补充 slash 命令、usage 面板与 store 的行为测试。
- 调整会话视图“回到底部”悬浮按钮的主题样式，复用悬浮指示器变量并增强暗黑模式下的边界与阴影对比。
- 新增编辑器定位测试，验证富文本 Markdown 导入导出会转义 HTML 注释、聚焦本身不会回写内容，以及标题 ID 补齐事务不会改写 Markdown 字符串。
- 修复富文本编辑器对整段 HTML 注释的 Markdown 序列化，避免 `<!-- comment -->` 在无用户编辑时被转义为 HTML 实体。
- 新增真实文档诊断测试，确认 `2026-04-26-sessionhistory-virtual-scroll-design.md` 会在聚焦后因表格 Markdown 规范化触发一次 `update:value` 回写。
- 修复富文本编辑器在表格结尾自动插入尾随空段落的问题，避免设计文档在仅聚焦时因多出 `\n\n` 而触发 `update:value` 回写。
- 新增 BEditor 聚焦意外回写问题复盘文档，补充问题现象、根因分析、解决思路、验证方式与后续建议。
