# 2026-05-02

## Added
- 新增聊天文件引用结构化资源重构设计文档，明确 `file-reference part`、`read_reference` 工具与 snapshot 读取语义。
- 新增聊天文件引用结构化资源实施计划文档，拆分消息结构、snapshot、工具和模型上下文改造任务。
- 新增聊天语音输入设计文档，明确独立 `VoiceInput` 组件、录音波形、边录边转写、多段拼接和 `whisper.cpp` 主进程转写方案。

## Changed
- 补充文件引用后续实现方向，明确模型上下文从正文预展开切换为结构化引用索引。
