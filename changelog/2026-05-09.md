# 2026-05-09

## Fixed
- 修复拖拽标签到第一个位置时插入指示线不显示的问题（为 .header-tabs 容器添加左侧 padding）

## Added
- 新增 `docs/superpowers/plans/2026-05-09-speech-runtime-v2.md`，拆解 Chat 语音运行时 V2 设计的实施阶段、涉及文件、测试范围与迁移收尾任务
- 新增 `electron/main/modules/speech/catalog.mts`，支持语音运行时 V2 catalog manifest 解析、官方模型目录查询与更新检查
- 新增 `test/electron/speech-runtime.test.ts` 与 `test/electron/speech-catalog.test.ts`，覆盖语音运行时 V2 状态解析、外部模型能力与 catalog 更新判断
- 新增 `test/electron/speech-ipc.test.ts`，覆盖旧 `speech:installRuntime` 兼容入口的 binary 与推荐模型补齐语义
- 新增 `resources/speech/manifest.json` 的 V2 catalog 结构，以及对应的脚本/安装器回归用例

## Changed
- 重构 `electron/main/modules/speech/runtime.mts`，改为基于 `speech-runtime/state.json` 解析 binary、官方模型、外部模型与当前选择
- 重构 `electron/main/modules/speech/installer.mts`，支持 versioned binary/model 仓库、binary 更新下载、应用与回滚
- 更新 `electron/main/modules/speech/ipc.mts`、`electron/preload/index.mts` 与 `types/electron-api.d.ts`，暴露 speech runtime snapshot、catalog、外部模型与更新管理接口
- 更新 `src/views/settings/speech/index.vue`，接入语音 catalog 模型列表、更新检查、binary 更新下载/应用/回滚入口，以及官方模型安装、删除与设为当前操作
- 更新 `src/components/BChatSidebar/components/InputToolbar/VoiceInput.vue`，按 V2 语义区分 binary 缺失、无模型与当前选择失效的语音输入兜底逻辑
- 更新 `src/views/settings/speech/index.vue`，补齐外部模型添加、重命名、重新校验、删除与设为当前的设置页管理操作
- 更新 `electron/main/modules/speech/runtime.mts`，补齐 V1 `manifest.json` 与 `current/` 目录向 V2 `state.json` 和版本化仓库的一次性迁移逻辑
- 更新 `electron/main/modules/speech/ipc.mts`，将旧 `speech:installRuntime` 过渡为“确保 binary 就绪，并在缺少可用模型时自动安装并设为当前的推荐官方模型”
- 更新 `scripts/speech/manifest-tool.mjs` 与 `scripts/speech/dev-runtime.mjs`，支持 V2 `schemaVersion + binaries + models` manifest，同时保留对旧结构的兼容处理
- 更新 `electron/main/modules/speech/installer.mts`，支持从 V2 catalog manifest 还原旧安装入口所需的兼容清单
