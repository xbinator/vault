# 2026-04-24

## Added
- 新增文件引用聊天上下文设计文档，明确 `references`、`snapshot`、`summary` 三层数据模型与 `documentId` 主键方案。
- 新增文件引用上下文实现计划文档，拆分持久化、稳定 token、聊天渲染与模型上下文构建等阶段任务。
- 新增 `src/ai/tools` 新工具开发指南文档，整理工具定义、权限接入、默认暴露和测试补充流程。
- 新增 `read_directory` 内置工具和底层工作区目录读取能力，支持安全读取目录的直接子项列表。

## Changed
- 明确文件引用消息的固定三档上下文注入阈值，并约束 `excerpt` 仅用于 UI 预览，不参与模型上下文构建。
- 补充文件引用上下文设计文档中的快照复用粒度、孤儿数据策略、`line` 解析契约、token 草稿级还原范围与阶段拆分边界。
- 补充 `parseLineRange` 的 1-based 行号约定与解析失败降级策略，并恢复 `fallback`、实现阶段和测试建议章节。
- 持久化聊天消息中的 `references` 元数据，支持会话存储与读取时保留文件引用信息。
- 调整 `BPromptEditor` 文件引用 chip 的序列化逻辑，改为基于稳定 `referenceId` 的 `{{file-ref:...}}` token，并将引用查找范围收敛到当前编辑器实例。
- 调整 Electron 主进程 logger 控制台序列化深度，避免深层消息内容被折叠成 `[Array]`，并补充对应回归测试。
- 统一 `src/ai/tools/builtin/catalog.ts` 的默认工具清单命名来源，改为引用各内置工具导出的共享工具名常量，减少字符串散落。
- 将 `get_settings` 补入默认只读工具清单，并统一使用 `GET_SETTINGS_TOOL_NAME` 常量参与目录与测试断言。
