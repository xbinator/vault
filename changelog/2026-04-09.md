# 2026-04-09

## Changed
- 修复服务模型配置切换到 SQLite 后缺少旧 localforage 数据迁移的问题，并为非 Tauri 环境恢复浏览器存储回退。
- 修复编辑器选区 AI 入口与输入框对模型配置变化感知不及时的问题。
- 修复选区 AI 输出过程中重复写入导致正文被破坏的问题，改为完成流式接收后统一替换选区。
- 优化服务模型配置卡片的背景层级与分组区块样式，提升白色界面下的信息分层和可读性。
- 统一编辑器选区 AI 相关组件获取服务模型配置的逻辑，收敛到 `src/stores/service-model.ts`，并对模型可用性校验保持一致。
- 修复 Electron 打开文件后文件名重复拼接扩展名的问题，恢复与 Tauri 版本一致的文件名解析语义。
- 为 Electron 新增 Tauri 最近文件与当前文件记录迁移，并将桌面端最近文件存储切换到可控的原生存储，避免升级后历史记录丢失。
- 开始收敛目录结构，将平台能力与存储能力迁移到 `src/shared/platform` 和 `src/shared/storage`，并保留 `src/utils` 兼容导出层。
- 调整 TypeScript 配置以消除当前版本的弃用项告警，恢复类型检查可用性。
- 删除目录调整后已无引用的 `src/utils/native` 与 `src/utils/storage` 兼容层文件，减少历史路径残留。
- 开始将编辑器业务从 `src/views/editor` 迁移到 `src/modules/editor`，为后续按业务域收敛目录结构做准备。
- 开始将设置相关业务从 `src/views/settings` 迁移到 `src/modules/settings`，继续按业务域收敛目录结构。
- 开始将 AI 服务能力从 `src/services/ai` 迁移到 `src/modules/ai/services`，继续按业务域整理代码布局。
- 清理已空置的 `src/views`、`src/services` 及其他无文件无引用目录，进一步收敛目录结构。
- 调整页面目录职责：恢复 `src/views` 作为路由视图入口，并将 `src/modules` 中的页面实现下沉到各业务域的 `pages/` 目录，重新拆分视图层与模块层。
- 取消 `src/modules` 目录分层，将编辑器与设置相关代码回迁到 `src/views`，并将 AI 服务能力恢复到 `src/services/ai`，保持目录职责更直接。
- 修复 Electron 开发链路：为主进程与预加载脚本补充独立的 TypeScript 构建配置，并修正 ESM 模式下的路径解析，避免 `electron:dev` 长时间卡在 `wait-on`。
- 调整 Electron 主进程与预加载脚本的构建产物为独立 CommonJS 运行环境，确保 preload 能稳定注入 `window.electronAPI`。
- 移除全局 `electron.d.ts` 声明文件，改为通过显式的 `electron-api` helper 统一访问和收敛 Electron 桥接类型。
- 清理无引用文件与本地残留：删除未使用的 `src/assets/vue.svg`、根目录 `components.d.ts` 以及仓库中误入版本控制的 `.DS_Store` 文件。
- 继续清理本地残留文件，删除 `src/components/BPromptEditor/.DS_Store`。
